schema_version: 1
project: Property Assistant
repository: sp4devops/realestate-agent
status: bootstrap
active_phase: P0
active_phase_name: Harness and source-of-truth bootstrap
last_verified_commit: UNVERIFIED
last_updated: 2026-08-21T18:57:00+05:30
active_pr: 1
active_branch: ai/bootstrap-project-harness

ci_gate:
  status: blocked
  head_sha: 366211346d74b5bef15b3587a887304cc533e106
  required_for_advance: true
  observed_issue: Earlier harness runs failed because the validator used a formatting-brittle literal phrase check. That defect is fixed. Current connector run lookup cannot retrieve push-triggered runs for this private branch, so terminal success for current HEAD cannot yet be observed here.
  terminal_success_required: true

product_contract:
  core_loop: [capture, understand, remember, match, act]
  primary_user_term: Property Advisor
  internal_legal_term: Real Estate Agent
  platforms_now: [android, desktop]
  platforms_later: [ios]
  launch_languages: [Tamil, English, Tanglish]
  local_first: true
  model_optional_for_core_workflows: true
  target_android_ram_gb: 4

phases:
  P0:
    name: Harness and source-of-truth bootstrap
    status: in_progress
    acceptance:
      - AGENTS.md defines session, testing, review, handoff, privacy, Git, and CI completion-loop rules
      - machine-readable state exists
      - delivery phases and quality gates exist
      - CI validates harness integrity
      - product guardrails are represented in-repo
      - approved prototype is represented as the visual acceptance contract
  P1: {name: Application skeleton and navigation, status: pending}
  P2: {name: Local domain model and persistence, status: pending}
  P3: {name: Type capture and structured extraction, status: pending}
  P4: {name: Voice capture and multilingual understanding, status: pending}
  P5: {name: Matching and action brain, status: pending}
  P6: {name: Ask/search and operational result cards, status: pending}
  P7: {name: Poster capture and OCR lead flow, status: pending}
  P8: {name: After-call recap follow-ups and communication actions, status: pending}
  P9: {name: Backup restore settings language and privacy hardening, status: pending}
  P10: {name: Performance accessibility resilience and offline QA, status: pending}
  P11: {name: Android APK and desktop release candidates, status: pending}
  P12: {name: Broker pilot readiness, status: pending}

current_tasks:
  - id: P0-T1
    status: done
    owner: ai-agent
    description: Install repository AI harness and progressive CI quality gates.
  - id: P0-T2
    status: pending
    owner: ai-agent
    description: Add exact approved product-charter and visual-prototype source artifacts byte-for-byte when the active repository interface supports practical transfer.
  - id: P0-T3
    status: in_progress
    owner: ai-agent
    description: Drive the CI completion loop until the current HEAD is terminal green, then run code/QA/product reviews before advancing.

blockers:
  - id: P0-B1
    severity: medium
    description: The connector can create UTF-8 repository files but does not provide direct upload of the two attached source files; concise locked guardrails and visual acceptance contract are committed instead.
  - id: P0-B2
    severity: high
    description: Current ChatGPT GitHub run lookup only exposes pull-request-triggered workflow runs and cannot observe this branch's push-triggered run; phase advancement is blocked until terminal CI for current HEAD is observable. Repo-native scripts/ci_wait.sh provides blocking wait/log behavior for agents with gh CLI access.

known_constraints:
  - Cash build-cost target is zero until product validation/revenue threshold.
  - No paid AI APIs or cloud dependency should be introduced merely for convenience.
  - Normal Android apps must not assume unrestricted cellular-call audio capture/transcription.
  - UI display language must remain independent from voice/input language.

last_checks:
  - check: repository access
    result: pass
    evidence: GitHub connector has admin/push access to private repository.
  - check: draft pull request
    result: pass
    evidence: PR #1 remains open and mergeable.
  - check: CI failure diagnosis
    result: fail_then_fixed
    evidence: scripts/harness_check.py used a literal semantic phrase that Markdown formatting split; validator now normalizes formatting and punctuation.
  - check: agent control loop
    result: pass_design
    evidence: AGENTS.md and docs/engineering/AGENT_EXECUTION_LOOP.md require push -> discover -> poll -> completed -> inspect -> fix -> repeat and prohibit phase advancement without current-HEAD green CI.
  - check: blocking watcher
    result: added
    evidence: scripts/ci_wait.sh uses gh run watch --exit-status and prints failed logs; non-green or missing CI exits non-zero.

next_actions:
  - From an environment with authenticated gh CLI, run scripts/ci_wait.sh for current HEAD and block until completion.
  - If failed, inspect emitted logs, fix the root cause, push, and rerun the watcher.
  - When CI is green, run separate code reviewer, QA reviewer, and product-guardrail reviewer passes.
  - Resolve Critical/High findings and repeat CI after fixes.
  - Mark P0 complete only after current HEAD has terminal green CI plus green review gates.
